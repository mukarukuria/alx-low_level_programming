@include "main.h"

/**
 * main - Entry point
 * Description: Prints alphabets in small letters with a new line at the end
 * Return: Always 0 (Success)
 */

int main(void)
{
	print_alphabet();
	return (0);
}
